Menu docs
---
tags:
  - Menu
parameters:
  - name: Authorization
    in: header
    type: string
    description: JWT token authorization
    required: true
    example: "Bearer "
description: This endpoint is for user to add item to menu

responses:
  200:
    description: This returns a list of all items on the menu
  400:
    description: Empty item_name, item_description, item_price
